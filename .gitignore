# OS / editor
.DS_Store
.vscode/

# Secrets / env
.env
.env.*
!.env.example
server/navigator_api_keys.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Python test/cache
.pytest_cache/
.coverage

# React / Node frontend
node_modules/
client/.vite/
dist/
build/

# Logs / local run state
*.log
.run/

# Local fetched Invite Hub artifacts (DEFAULT_STATE_PATH / DEFAULT_OUTPUT_PATH
# resolve next to the ingest module; server/state/ holds the prod-mounted cursor)
server/vex_agent/ingest/invite_hub_sync_state*.json
server/vex_agent/ingest/invite_vex_logs*.ndjson
server/state/
